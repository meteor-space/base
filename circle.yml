machine:
  node:
    version: 4.0.0
  pre:
    - curl https://install.meteor.com | /bin/sh
dependencies:
  cache_directories:
    - node_modules
  pre:
    - npm install -g spacejam@rc

test:
  override:
    - spacejam test-packages --driver-package=practicalmeteor:mocha-console-runner ./
