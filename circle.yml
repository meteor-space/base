machine:
  node:
    version: 4.0.0
  pre:
    - curl https://install.meteor.com | /bin/sh
dependencies:
  cache_directories:
    - node_modules

test:
  override:
    - mocha --require babel-core/register --recursive test/**/*.js

