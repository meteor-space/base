machine:
  node:
    version: 4.0.0

dependencies:
  override:
    - npm install
  cache_directories:
    - node_modules

test:
  override:
    - mocha --require babel-core/register --recursive test/**/*.js
